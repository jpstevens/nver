language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-node-module-coffee' ]; then cd .. && eval "mv $currentfolder generator-node-module-coffee" && cd generator-node-module-coffee; fi

